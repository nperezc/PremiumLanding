# Instrucciones permanentes del proyecto

- **Idioma de las respuestas: español.** Toda conversación con el usuario
  se responde en español, siempre. (Instrucción dada por el usuario el
  11-sep-2026; no eliminar.)

## Contexto rápido

- El Hero de `index.html` está aprobado y congelado: no modificar su
  marcado, clases ni lógica de animación. Es la fuente de verdad del
  sistema visual (paleta, Lexend, easings, ventanas redondeadas).
- Sin bundler ni npm: scripts clásicos cargados por `<script>` en orden
  de dependencia (ver `ARCHITECTURE.md`).
